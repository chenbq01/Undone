<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
	xmlns:beans="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="
		http://www.springframework.org/schema/beans
		http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/security
		http://www.springframework.org/schema/security/spring-security.xsd">

	<beans:bean
		class="org.season.autumn.springsecurity.security.IPTokenBasedRememberMeServices"
		id="ipTokenBasedRememberMeServicesBean">
		<beans:constructor-arg name="key" value="autumn" />
		<beans:constructor-arg name="userDetailsService"
			ref="userService" />
		<beans:constructor-arg name="parameter" value="_remember_me" />
		<beans:constructor-arg name="cookieName" value="REMEMBER_ME_COOKIE" />
	</beans:bean>

	<http auto-config="true" use-expressions="true">
		<intercept-url pattern="/account/*"
			access="hasRole('ROLE_USER') and fullyAuthenticated" />
		<intercept-url pattern="/login" access="permitAll" />
		<intercept-url pattern="/*" access="hasRole('ROLE_USER')" />
		<form-login login-page="/login" />
		<logout invalidate-session="true" logout-success-url="/"
			logout-url="/logout" />
		<remember-me key="autumn" services-ref="ipTokenBasedRememberMeServicesBean" />
	</http>

	<!--authentication-manager alias="authenticationManager"> <authentication-provider> 
		<user-service id="userService"> <user authorities="ROLE_USER" name="guest" 
		password="guest" /> </user-service> </authentication-provider> </authentication-manager -->

	<authentication-manager alias="authenticationManager">
		<authentication-provider user-service-ref="userService" />
	</authentication-manager>

	<beans:bean id="userService"
		class="org.season.autumn.springsecurity.security.InMemoryChangePasswordDaoImpl">
		<beans:property name="userProperties">
			<beans:props>
				<beans:prop key="guest">guest,ROLE_USER</beans:prop>
			</beans:props>
		</beans:property>
	</beans:bean>

</beans:beans>